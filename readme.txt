# The R docker image name is rbase and not r
# This is because earlier versions of docker
# did not allow image names of length one.
# I think it does now, but I'm keeping the
# old name for backward compatibility, eg
# forking from an older kata.
